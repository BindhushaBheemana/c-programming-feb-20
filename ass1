//Find Lowest among n numbers
#include<stdio.h>
void main()
{
	int n,i,ele,h;
	scanf("%d",&n);  // for how many numbers
	i=1;
	while(i<=n)
	{
		scanf("%d",&ele);
		if(i==1) 
	        h=ele;
		if(ele>h)
			h=ele;
		i++;
	}
	printf("\nHighest %d",h);
}
